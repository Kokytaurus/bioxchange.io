# bioxchange.io
Install node and npm (node package manager) 
Create package.json file as this is a react app and uses npm
Use $ npm start to access development build
use $ npm run build to set up app for development
Endeavor to make changes to content for unique ouput
